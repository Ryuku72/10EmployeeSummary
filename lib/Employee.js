// TODO: Write code to define and export the Employee class

class Employee {
    constructor(name, id, gender, age, email, role) {
        this.name = name
        this.id = id;
        this.gender = gender;
        this.age = age;
        this.email = email;
        this.role = role;
    }
    getName(){
        name = `${data.name}`
    }
    getId(){
        id = `${data.id}`
    }
    getGender(){
        gender = `${data.gender}`
    }
    getAge(){
        age = `${data.age}`
    }
    getEmail(){
        email = `${data.email}`
    }     
    getRole(){
        role= `${data.role}`
    }
    
}

module.exports = Employee;
